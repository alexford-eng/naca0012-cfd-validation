FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createPatchDict;
}

pointSync false;

patches
(
    {
        name aerofoil;
        patchInfo { type wall; }
        constructFrom patches;
        patches (auto3);
    }
    {
        name farfield;
        patchInfo { type patch; }
        constructFrom patches;
        patches (auto4);
    }
    {
        name outlet;
        patchInfo { type patch; }
        constructFrom patches;
        patches (auto0);
    }
    {
        name frontAndBack;
        patchInfo { type empty; }
        constructFrom patches;
        patches (auto1 auto2);
    }
);
